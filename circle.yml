machine:
    python:
        version: 3.5.2
dependencies:
    pre:
        - pip install -r requirements.txt
test:
    override:
        - nosetests

deployment:
  staging:
    branch: refactor-OOP
    commands:
      - git fetch origin --unshallow