machine:
    python:
        version: 3.5.2
    environment:
    COVERALLS_REPO_TOKEN: shoJ2efCbibqopt0EBZhyyVnCVA7G92ZV
    COVERALLS_PARALLEL: true

notify:
  webhooks:
    - url: https://coveralls.io/webhook?repo_token=shoJ2efCbibqopt0EBZhyyVnCVA7G92ZV

dependencies:
    pre:
        - pip install -r requirements.txt
        - pip install coveralls
test:
    override:
        - nosetests --with-coverage --cover-package=amity
    post:
        - coveralls

deployment:
  staging:
    branch: refactor-OOP
    commands:
      - git fetch origin --unshallow
